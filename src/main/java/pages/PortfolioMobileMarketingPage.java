package pages;

import org.openqa.selenium.WebDriver;

public class PortfolioMobileMarketingPage extends PageGeneralFunctions {

    private WebDriver driver;

    public PortfolioMobileMarketingPage(WebDriver driver) {
        super(driver);
        this.driver = driver;
    }
}
